<nav class="red lighten-1">
  <div class="row">
    <div class="col s12">
      <div class="nav-wrapper">
        <div class="col s12">
         {{#link-to 'auth.dashboard' class="breadcrumb"}}My English Academy {{/link-to}}
         {{#link-to 'auth.subscribers' class="breadcrumb"}}Subscriptores {{/link-to}}
         <a href="" class="breadcrumb"> Nuevo </a>
       </div>
       </div>
    </div>
  </div>
</nav>

<div class="row">
  <div class="col s10 offset-s1">
    <h4> Nuevo Subscriptor </h4>
  </div>
</div>

<div class="row">
  <form class="col s10 offset-s1" {{action 'saveSubscriber' model on='submit'}}>
      <div class="row">
        <div class="input-field col s6">
          {{input type="text" class="validate active" value=model.name}}
          <label>Nombre</label>
        </div>
        <div class="input-field col s6">
          {{input type="text" class="validate active" value=model.last_name}}
          <label>Apellidos</label>
        </div>
      </div>
      <div class="row">
        <div class="input-field col s12">
          {{input type="number" type="text" class="validate" value=model.phone}}
          <label>Teléfono</label>
        </div>
      </div>
      <div class="row">
        <div class="input-field col s12">
          {{input type="email" type="text" class="validate" value=model.mail}}
          <label>Email</label>
        </div>
      </div>
      <!-- <div class="row">
        <div class="input-field col s12">
          {{#power-select
            options=model.contracts
            placeholder='Contratos'
            onchange=(action "foo")
            as |contract|}}
            {{contract}}
          {{/power-select}}
        </div>
      </div> -->
      <div class="row">
        {{#link-to 'auth.subscribers' class="waves-effect waves-light btn red left"}}
          <i class="material-icons right">cancel</i> Cancelar
        {{/link-to}}
        <button type="submit" class="waves-effect waves-light btn blue darken-1 right">
          <i class="material-icons right">save</i> Guardar
        </button>
      </div>
  </form>
</div>
