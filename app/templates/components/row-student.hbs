<div class="row">
  <div class="col s8 offset-s2">
    <ul class="collection with-header">
      <li class="collection-header"><h6>Contratos del Alumno</h6></li>
      {{#each studentContracts as |studentContract|}}
        <li class="collection-item avatar">
          <img src="/media/contract.png" alt="" class="circle">
          <span class="title">{{studentContract.student.fullName}}</span>
          <p>Agregado en {{studentContract.startDateString}} </p>
          <a class="secondary-content btn-floating left blue darken-2"><i class="material-icons">edit</i></a>
        </li>
      {{else}}
        <li class="collection-item">No se ha agregado al alumno a ningún contrato.</li>
      {{/each}}
    </ul>
  </div>
</div>
